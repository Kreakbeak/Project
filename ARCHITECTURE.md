# System Architecture & Workflow

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React)                         │
│                    http://localhost:3000                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │   Farmer     │  │   Admin      │  │  Auth       │           │
│  │  Dashboard   │  │  Dashboard   │  │  Pages      │           │
│  └──────────────┘  └──────────────┘  └──────────────┘           │
│        │                  │                  │                   │
│        └──────────────────┼──────────────────┘                   │
│                           │                                      │
│                    React Router (SPA)                            │
│                           │                                      │
│                     API Client (axios)                           │
│                           │                                      │
└───────────────────────────┼──────────────────────────────────────┘
                            │
                    HTTP/REST API calls
                            │
┌───────────────────────────┼──────────────────────────────────────┐
│                   BACKEND (Node.js + Express)                    │
│                   http://localhost:5000/api                      │
│                                                                  │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐    │
│  │ Auth Routes    │  │ Report Routes  │  │ Middleware     │    │
│  │ /register      │  │ /reports       │  │ JWT verify     │    │
│  │ /login         │  │ /my-reports    │  │ RBAC check     │    │
│  └────────────────┘  └────────────────┘  └────────────────┘    │
│        │                    │                    │              │
│        └────────────────────┼────────────────────┘              │
│                             │                                   │
│  ┌────────────────────────────────────┐                        │
│  │ Controllers & Business Logic       │                        │
│  │ - authController                  │                        │
│  │ - reportController                │                        │
│  └────────────────────────────────────┘                        │
│        │                    │                                   │
│        └────────┬───────────┘                                   │
│                 │                                               │
│  ┌────────────────────────────────────┐                        │
│  │ Multer File Upload                 │                        │
│  │ - Validate files                   │                        │
│  │ - Store in /uploads/               │                        │
│  └────────────────────────────────────┘                        │
│                                                                │
│  ┌────────────────────────────────────┐                        │
│  │ Models (Mongoose)                  │                        │
│  │ - User Schema                      │                        │
│  │ - Report Schema                    │                        │
│  └────────────────────────────────────┘                        │
│                             │                                   │
└─────────────────────────────┼───────────────────────────────────┘
                              │
                        Data Operations
                              │
┌─────────────────────────────┼───────────────────────────────────┐
│                  MongoDB Database                               │
│                 (Atlas or Local)                                │
│                                                                │
│  ┌──────────────┐              ┌──────────────┐               │
│  │   Users      │              │   Reports    │               │
│  │  Collection  │              │  Collection  │               │
│  │              │              │              │               │
│  │ - _id        │              │ - _id        │               │
│  │ - name       │              │ - farmerId   │               │
│  │ - email      │              │ - cropType   │               │
│  │ - password   │              │ - imagePath  │               │
│  │ - phone      │              │ - description               │
│  │ - role       │              │ - location   │               │
│  │ - location   │              │ - status     │               │
│  │ - createdAt  │              │ - treatment  │               │
│  └──────────────┘              └──────────────┘               │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 🔄 User Flow Diagrams

### Farmer Flow
```
┌─────────────┐
│  Start      │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│ Register/Login  │
└──────┬──────────┘
       │
       ▼
┌──────────────────────┐
│ Farmer Dashboard     │
│ - Submit Report      │
│ - View My Reports    │
└──────┬───────────────┘
       │
       ├────────────┬──────────────┐
       ▼            ▼              ▼
   ┌─────────┐  ┌──────────┐  ┌──────────┐
   │ Submit  │  │ View All │  │ Logout   │
   │ Report  │  │ Reports  │  │          │
   └────┬────┘  └────┬─────┘  └──────────┘
        │            │
        │            ▼
        │       ┌────────────┐
        │       │ View Status│
        │       │ & Treatment│
        │       └────────────┘
        │
        ▼
   ┌──────────────┐
   │ Upload Image │
   │ + Details    │
   │ + Location   │
   └────┬─────────┘
        │
        ▼
   ┌──────────────┐
   │ Submit to    │
   │ Backend      │
   └────┬─────────┘
        │
        ▼
   ┌──────────────┐
   │ Report Status│
   │ = "Pending"  │
   └──────────────┘
```

### Admin Flow
```
┌─────────────┐
│  Start      │
└──────┬──────┘
       │
       ▼
┌──────────────────┐
│ Admin Register   │
│ or Login         │
└──────┬───────────┘
       │
       ▼
┌──────────────────────────┐
│ Admin Dashboard          │
│ - View All Reports       │
│ - Manage Reports         │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ View All Reports         │
│ (with farmer details)    │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Filter by Status         │
│ - Pending                │
│ - Identified             │
│ - Resolved               │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Click View Report        │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Report Detail Page       │
│ - See farmer info        │
│ - See image              │
│ - See description        │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Update Status            │
│ Pending → Identified     │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Add Treatment            │
│ Recommendation           │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Click Update             │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Report Updated & Farmer  │
│ Sees Changes             │
└──────────────────────────┘
```

---

## 🔐 Authentication Flow

```
CLIENT                          BACKEND                     DATABASE
│                               │                              │
│─ POST /auth/register ────────▶│                              │
│ {name, email, password...}   │                              │
│                               │── Hash password             │
│                               │── Create user ──────────────▶│
│                               │                    │ save    │
│                               │◀─────────────────────────────│
│                               │                              │
│                               │── Sign JWT token            │
│◀── token (JWT) ───────────────│                              │
│                               │                              │
│─ POST /reports ──────────────▶│                              │
│ Authorization: Bearer token   │── Verify token             │
│ {report data + image}         │── Check role (farmer)      │
│                               │── Process upload           │
│                               │── Create report ──────────▶│
│                               │                    │ save   │
│                               │◀─────────────────────────────│
│◀── success response ──────────│                              │
│                               │                              │
```

---

## 📊 Report Status Lifecycle

```
┌─────────────┐
│   PENDING   │  <- Report submitted by farmer
│             │     Status: Awaiting expert review
└──────┬──────┘
       │
    Admin reviews
       │
       ▼
┌──────────────┐
│  IDENTIFIED  │  <- Pest/disease identified
│              │     Admin added treatment
└──────┬───────┘
       │
    Farmer implements
    treatment
       │
       ▼
┌──────────────┐
│  RESOLVED    │  <- Issue resolved
│              │     Treatment effective
└──────────────┘
```

---

## 🗂️ Database Relationships

```
┌──────────────────┐
│     USERS        │
│──────────────────│
│ _id (Primary Key)│
│ name             │
│ email (Unique)   │
│ password         │
│ phone            │
│ role (farmer)    │
│ location         │
│ createdAt        │
└────────┬─────────┘
         │ 1:Many
         │
         │
         │ farmerId (Foreign Key)
         ▼
┌──────────────────┐
│    REPORTS       │
│──────────────────│
│ _id (Primary Key)│
│ farmerId (FK)────┼──▶ User
│ cropType         │
│ imagePath        │
│ description      │
│ location         │
│ status           │
│ treatment        │
│ createdAt        │
│ updatedAt        │
└──────────────────┘
```

---

## 🔌 API Call Sequence

### Submit Report Sequence
```
1. User selects image → Browser validates
2. User clicks Submit → Frontend creates FormData
3. POST /api/reports with image → Multer receives
4. Multer validates & stores file → Returns filename
5. Controller saves report to DB → Returns report object
6. Frontend shows success → Redirects to My Reports
7. User can now see report with "Pending" status
```

### Update Report Sequence
```
1. Admin views report detail page
2. Admin selects status: "Identified"
3. Admin types treatment recommendation
4. Admin clicks "Update Report"
5. PUT /api/reports/:id with new data
6. Backend updates MongoDB record
7. Returns updated report
8. Frontend shows success message
9. Farmer logs in → Sees updated status & treatment
```

---

## 📈 Data Flow for Report

```
FARMER CREATES REPORT:
Image + Details
    │
    ▼
Form Validation (Frontend)
    │
    ▼
POST /api/reports
    │
    ▼
JWT Verification
    │
    ▼
Role Check (farmer)
    │
    ▼
Multer Processing
    ├── Validate file type (image only)
    ├── Validate file size (<5MB)
    └── Store in /uploads/
    │
    ▼
Controller Processing
    ├── Extract data
    ├── Get image path
    └── Create report object
    │
    ▼
Save to MongoDB
    │
    ▼
Return report JSON
    │
    ▼
ADMIN VIEWS & UPDATES:
GET /api/reports/:id (with auth)
    │
    ▼
Retrieve from MongoDB
    │
    ▼
Return report with farmer details
    │
    ▼
Admin reviews & updates
    │
    ▼
PUT /api/reports/:id
    ├── Verify admin role
    ├── Update status
    └── Update treatment
    │
    ▼
Save to MongoDB
    │
    ▼
Return updated report
    │
    ▼
FARMER SEES UPDATE:
GET /api/reports/my-reports
    │
    ▼
Reports with new status & treatment
```

---

## 🔄 Complete Request-Response Cycle

```
┌─────────────────────────────────────────────────────────────┐
│ FRONTEND (React)                                            │
│ User interaction → State update                             │
└────────────────┬────────────────────────────────────────────┘
                 │
                 │ axios HTTP Request
                 ▼
┌─────────────────────────────────────────────────────────────┐
│ NETWORK LAYER                                               │
│ HTTP/HTTPS Communication                                    │
└────────────────┬────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────┐
│ BACKEND (Express Server)                                    │
│                                                             │
│ 1. Receive Request                                          │
│ 2. CORS middleware check                                    │
│ 3. Parse JSON/FormData                                      │
│ 4. Route matching                                           │
│ 5. Auth middleware (JWT verify)                             │
│ 6. Authorization (role check)                               │
│ 7. Validation (express-validator)                           │
│ 8. Multer (if file upload)                                  │
│ 9. Controller logic                                         │
│ 10. Database operation (Mongoose)                           │
│ 11. Response formulation                                    │
└────────────────┬────────────────────────────────────────────┘
                 │
                 │ JSON Response
                 ▼
┌─────────────────────────────────────────────────────────────┐
│ FRONTEND (React)                                            │
│ Receive response → Update state → Re-render UI              │
└─────────────────────────────────────────────────────────────┘
```

---

## 📋 State Management Flow

```
User Login
    │
    ▼
Success → Store token in localStorage
    │
    ▼
getAuthToken() reads localStorage
    │
    ▼
axios interceptor adds token to headers
    │
    ▼
Protected API calls succeed
    │
    ▼
Response data → React state update
    │
    ▼
UI re-renders with data

User Logout
    │
    ▼
Remove token from localStorage
    │
    ▼
Clear axios default headers
    │
    ▼
Redirect to login page
```

---

## 🎯 Key Components Overview

```
Frontend Components:
├── Auth Pages
│   ├── Login.js          → Form + API call
│   └── Register.js       → Form + API call
│
├── Farmer Pages
│   ├── FarmerDashboard   → Navigation hub
│   ├── SubmitReport      → Form + image upload
│   └── MyReports         → List + status display
│
├── Admin Pages
│   ├── AdminDashboard    → Navigation hub
│   ├── AdminReports      → Filtered list
│   └── ReportDetail      → View + update form
│
└── Utilities
    └── api.js            → Axios configuration

Backend Components:
├── Routes
│   ├── auth.js          → Login/Register endpoints
│   └── reports.js       → CRUD endpoints
│
├── Controllers
│   ├── authController   → Business logic for auth
│   └── reportController → Business logic for reports
│
├── Models
│   ├── User.js          → Schema + methods
│   └── Report.js        → Schema + methods
│
├── Middleware
│   └── auth.js          → JWT + RBAC
│
└── Config
    ├── db.js            → MongoDB connection
    └── multer.js        → File upload config
```

---

This comprehensive system architecture ensures scalability, security, and maintainability!
